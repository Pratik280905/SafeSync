# Copied from RocketRide extension settings when the engine is connected.
# Do not commit real keys. Use the RocketRide sidebar to set host URL and API key.

ROCKETRIDE_URI=ws://localhost:5565
ROCKETRIDE_APIKEY=

# Optional pipeline variables (prefix must be ROCKETRIDE_)
# ROCKETRIDE_GROQ_API_KEY=
# ROCKETRIDE_INPUT_PATH=
# ROCKETRIDE_OUTPUT_PATH=
